/**
 * Touch-target sizing — Apple HIG minimums and default hit-slop.
 */
export const TOUCH_TARGET = {
  min: 44,
  hitSlop: 12,
} as const

export const HIT_SLOP = {
  top: 12,
  left: 12,
  right: 12,
  bottom: 12,
} as const
