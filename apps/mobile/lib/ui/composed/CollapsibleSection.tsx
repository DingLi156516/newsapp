/**
 * CollapsibleSection — re-export of the existing molecule under the
 * `@/lib/ui` namespace. The component itself stays in
 * `components/molecules/` until a later phase physically relocates it.
 */

export { CollapsibleSection } from '@/components/molecules/CollapsibleSection'
