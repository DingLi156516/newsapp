/**
 * EmptyState — re-export of the existing `EmptyStateView` molecule under
 * the `@/lib/ui` namespace. The component itself stays in
 * `components/molecules/` until Phase 6 physically relocates it.
 */

export { EmptyStateView as EmptyState } from '@/components/molecules/EmptyStateView'
